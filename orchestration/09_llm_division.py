"""
Workshop 解答：LLM 與 Rule / ML / API 的分工

題目：
針對以下功能，選擇適合的技術：
1. 偵測安全帽
2. 判斷違規嚴重程度
3. 生成告警訊息
4. 查詢法規條文
5. 發送 Email 通知

執行方式：
python 09_llm_division.py
"""


def main():
    print("=== LLM 與其他技術的分工 ===\n")

    # === 技術選擇分析 ===

    print("1. 各功能技術選擇")
    print("-" * 50)

    analysis = """
┌─────────────────┬──────────────┬─────────────────────────────────┐
│ 功能            │ 推薦技術      │ 理由                            │
├─────────────────┼──────────────┼─────────────────────────────────┤
│ 1. 偵測安全帽   │ ML (YOLO)    │ 專門訓練的物件偵測模型，        │
│                 │              │ 速度快、準確率高、成本低         │
├─────────────────┼──────────────┼─────────────────────────────────┤
│ 2. 判斷嚴重程度 │ Rule + LLM   │ 基本規則固定（如：未戴安全帽=高）│
│                 │              │ 複雜情況用 LLM 輔助判斷          │
├─────────────────┼──────────────┼─────────────────────────────────┤
│ 3. 生成告警訊息 │ LLM          │ 需要自然語言生成能力，          │
│                 │              │ LLM 擅長生成流暢、專業的文字     │
├─────────────────┼──────────────┼─────────────────────────────────┤
│ 4. 查詢法規條文 │ RAG (搜尋+LLM)│ 結合向量搜尋和 LLM：            │
│                 │              │ 搜尋找到相關條文，LLM 回答問題   │
├─────────────────┼──────────────┼─────────────────────────────────┤
│ 5. 發送 Email   │ API          │ 純粹的執行動作，不需要 AI，     │
│                 │              │ 用 API 直接呼叫郵件服務          │
└─────────────────┴──────────────┴─────────────────────────────────┘
"""
    print(analysis)

    # === 詳細說明 ===

    print("\n2. 詳細說明")
    print("-" * 50)

    details = """
【1. 偵測安全帽 → ML (YOLO)】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

為什麼選 YOLO 而不是 LLM？

性能比較：
┌──────────────┬─────────────┬─────────────┐
│ 指標          │ YOLO        │ Gemini      │
├──────────────┼─────────────┼─────────────┤
│ 處理速度      │ 30-60 FPS   │ 1-3 秒/張   │
│ 每張成本      │ ~$0         │ ~$0.01      │
│ 準確率        │ 95%+        │ 85-90%      │
│ 即時性        │ 可          │ 不可        │
│ 離線運作      │ 可          │ 不可        │
└──────────────┴─────────────┴─────────────┘

結論：即時偵測任務用 ML 模型

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【2. 判斷嚴重程度 → Rule + LLM】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

混合策略：

```python
def judge_severity(violation):
    # 第一層：規則判斷（快速、確定）
    rule_based_severity = {
        "no_helmet": "high",
        "no_vest": "medium",
        "blocked_path": "medium",
        "messy_area": "low"
    }

    # 基本情況用規則
    if violation.type in rule_based_severity:
        base_severity = rule_based_severity[violation.type]
    else:
        base_severity = "medium"

    # 第二層：複雜情況用 LLM
    if violation.needs_complex_analysis:
        llm_analysis = llm.analyze(
            f"分析此違規的嚴重程度：{violation.description}"
            f"考慮因素：位置={violation.zone}, 時間={violation.time}"
        )
        return combine_judgments(base_severity, llm_analysis)

    return base_severity
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【3. 生成告警訊息 → LLM】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

為什麼用 LLM？

```python
# 方法 A：模板（不用 LLM）
template = "在{zone}發現{violation_type}違規，請立即處理。"
message = template.format(zone="施工區A", violation_type="未戴安全帽")
# 輸出：在施工區A發現未戴安全帽違規，請立即處理。
# 缺點：生硬、缺乏細節

# 方法 B：LLM 生成
prompt = f'''
生成一則工安告警訊息：
- 違規類型：{violation.type}
- 位置：{violation.zone}
- 時間：{violation.time}
- 相關法規：{violation.regulation}

要求：專業、清楚、包含建議行動
'''
message = llm.generate(prompt)
# 輸出：【工安警報】2024/01/15 10:30 於施工區A，偵測到人員未配戴安全帽，
#       違反職業安全衛生設施規則第280條。請立即要求相關人員配戴安全帽，
#       並通知現場主管進行安全確認。
# 優點：專業、完整、有細節
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【4. 查詢法規條文 → RAG】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

RAG = 向量搜尋 + LLM

```python
def query_regulation(question):
    # 步驟 1：向量搜尋（找相關條文）
    query_embedding = embedding_model.encode(question)
    relevant_docs = vector_db.search(query_embedding, top_k=3)

    # 步驟 2：LLM 回答（基於找到的條文）
    prompt = f'''
    根據以下法規條文回答問題：

    {relevant_docs}

    問題：{question}

    請引用具體條文編號回答。
    '''
    answer = llm.generate(prompt)
    return answer
```

為什麼不直接問 LLM？
- LLM 可能產生幻覺（編造不存在的法規）
- RAG 確保答案基於真實資料

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【5. 發送 Email → API】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

純粹執行動作，不需要 AI：

```python
def send_alert_email(recipient, subject, body):
    '''發送告警郵件 - 純 API 呼叫'''
    smtp_client.send(
        to=recipient,
        subject=subject,
        body=body
    )
    return {"status": "sent", "recipient": recipient}
```

可以包裝成 Tool 讓 Agent 呼叫，但執行本身不需要 AI。
"""
    print(details)

    # === 完整系統架構 ===

    print("\n3. 完整系統架構")
    print("-" * 50)

    architecture = """
技術分工架構圖：

┌────────────────────────────────────────────────────────────────┐
│                        監控畫面輸入                             │
└───────────────────────────┬────────────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────────────┐
│                    ML 層：物件偵測                              │
│  ┌─────────────┐                                               │
│  │   YOLO      │ → 偵測人員、安全帽、背心等                    │
│  │   模型      │   速度：30-60 FPS，成本：幾乎為零             │
│  └─────────────┘                                               │
└───────────────────────────┬────────────────────────────────────┘
                            │ 偵測結果
                            ▼
┌────────────────────────────────────────────────────────────────┐
│                    Rule 層：基礎判斷                            │
│  ┌─────────────┐                                               │
│  │   規則引擎   │ → 嚴重程度判斷、通知規則                     │
│  │   (if-else) │   速度：即時，成本：零                        │
│  └─────────────┘                                               │
└───────────────────────────┬────────────────────────────────────┘
                            │ 需要複雜分析？
                  ┌─────────┴─────────┐
                  │ No                │ Yes
                  ▼                   ▼
          ┌───────────┐      ┌────────────────────────────────┐
          │ 直接執行   │      │        LLM 層：智慧分析         │
          └───────────┘      │  ┌────────────┐ ┌───────────┐  │
                             │  │  Gemini    │ │  RAG      │  │
                             │  │  (分析)    │ │  (法規)   │  │
                             │  └────────────┘ └───────────┘  │
                             └───────────────┬────────────────┘
                                             │
                                             ▼
┌────────────────────────────────────────────────────────────────┐
│                    API 層：執行動作                             │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐              │
│  │  Email API  │ │   SMS API   │ │  資料庫 API │              │
│  └─────────────┘ └─────────────┘ └─────────────┘              │
└────────────────────────────────────────────────────────────────┘


成本效益分析：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

假設每天處理 10,000 張監控畫面：

┌─────────────┬─────────────────┬─────────────────┬──────────────┐
│ 功能         │ 全用 LLM        │ 分工架構         │ 節省         │
├─────────────┼─────────────────┼─────────────────┼──────────────┤
│ 物件偵測    │ $100/天         │ $0（本地 YOLO）  │ $100         │
│ 嚴重判斷    │ $50/天          │ $5（僅複雜案例） │ $45          │
│ 訊息生成    │ $50/天          │ $30（需要品質）  │ $20          │
│ 法規查詢    │ $20/天          │ $10（RAG 優化）  │ $10          │
│ 發送通知    │ $0              │ $0              │ $0           │
├─────────────┼─────────────────┼─────────────────┼──────────────┤
│ 總計        │ $220/天         │ $45/天          │ 節省 80%     │
└─────────────┴─────────────────┴─────────────────┴──────────────┘
"""
    print(architecture)

    print("\n=== 完成 ===")


if __name__ == "__main__":
    main()
