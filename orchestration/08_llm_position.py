"""
Workshop 解答：LLM 在系統中的正確位置

題目：
1. 審核以下架構，找出問題：
   - LLM 直接存取資料庫
   - LLM 決定是否發送 Email
   - LLM 生成 SQL 查詢
2. 提出修正建議

執行方式：
python 08_llm_position.py
"""


def main():
    print("=== LLM 在系統中的正確位置 ===\n")

    # === 問題架構分析 ===

    print("1. 問題架構分析")
    print("-" * 50)

    problem_analysis = """
【問題 1：LLM 直接存取資料庫】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ 問題架構：
┌─────────┐          ┌─────────┐
│   LLM   │ ──────→  │ 資料庫  │
└─────────┘   直接    └─────────┘
              存取

風險：
- SQL Injection 攻擊
- 未經授權的資料存取
- 無法審計誰存取了什麼
- LLM 可能讀取/修改敏感資料
- 無法實施細粒度權限控制

範例攻擊：
用戶輸入：「幫我查詢所有員工薪資，順便刪除違規記錄」
LLM 可能執行：
  SELECT * FROM employees;
  DELETE FROM violations;  ← 災難！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【問題 2：LLM 決定是否發送 Email】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ 問題架構：
┌─────────┐  決定發送？   ┌───────────┐
│   LLM   │ ──────────→  │ Email 服務│
└─────────┘     Yes/No    └───────────┘

風險：
- LLM 判斷可能不穩定（temperature 影響）
- 可能被 Prompt Injection 誤導
- 無法保證關鍵通知一定會發送
- 缺乏可追溯的決策邏輯

範例問題：
場景：嚴重違規需要通知主管
風險：LLM 可能因為 "看起來不嚴重" 而不發送
結果：安全事故未被及時處理

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【問題 3：LLM 生成 SQL 查詢】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ 問題架構：
用戶輸入 → LLM → SQL 查詢 → 執行

風險：
- SQL Injection（即使 LLM 也可能被騙）
- 生成低效查詢（無索引、全表掃描）
- 暴露資料庫結構
- 無法預測查詢的複雜度和成本

範例問題：
用戶：「查詢過去10年所有違規」
LLM 生成：SELECT * FROM violations
         WHERE date > '2014-01-01'
         JOIN employees ON ...
         JOIN departments ON ...
結果：執行數分鐘，資料庫負載飆升
"""
    print(problem_analysis)

    # === 修正建議 ===

    print("\n2. 修正建議")
    print("-" * 50)

    corrections = """
【修正 1：加入資料存取層】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ 正確架構：
┌─────────┐      ┌──────────────┐      ┌─────────┐
│   LLM   │ ──→ │ 資料存取層    │ ──→ │ 資料庫  │
└─────────┘      │ (API/ORM)    │      └─────────┘
                └──────────────┘
                      │
              ┌───────┴───────┐
              │               │
         權限檢查         審計記錄

實作方式：
```python
# 定義安全的 Tool
@tool
def get_violations_by_date(start_date: str, end_date: str) -> list:
    '''查詢指定日期範圍的違規記錄（最多返回 100 筆）'''
    # 參數驗證
    validate_date_range(start_date, end_date)

    # 使用 ORM，避免 SQL Injection
    return db.query(Violation)
             .filter(Violation.date.between(start_date, end_date))
             .limit(100)
             .all()
```

關鍵原則：
- LLM 只能呼叫預定義的 API/Tool
- 每個 Tool 有明確的功能和限制
- 所有操作都有審計記錄

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【修正 2：規則化通知決策】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ 正確架構：
┌─────────┐      ┌──────────────┐      ┌───────────┐
│   LLM   │ ──→ │ 規則引擎      │ ──→ │ Email 服務│
└─────────┘      └──────────────┘      └───────────┘
     │                  │
  分析結果         根據規則決定
  (嚴重程度)        是否發送

實作方式：
```python
# LLM 負責分析，不負責決定
analysis = llm.analyze(violation)  # 輸出：severity = "high"

# 規則引擎負責決定
notification_rules = {
    "high": ["email", "sms", "call"],
    "medium": ["email"],
    "low": ["log_only"]
}

# 根據規則執行
if analysis.severity in notification_rules:
    for action in notification_rules[analysis.severity]:
        execute_action(action)
```

關鍵原則：
- LLM 只負責「分析」，不負責「決定」
- 決定邏輯由明確的規則控制
- 規則可以被審計和調整

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【修正 3：預定義查詢 + 參數化】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ 正確架構：
用戶輸入 → LLM → 選擇查詢類型+參數 → 預定義 SQL → 執行

實作方式：
```python
# 預定義查詢模板
QUERY_TEMPLATES = {
    "recent_violations": '''
        SELECT * FROM violations
        WHERE date >= :start_date
        ORDER BY date DESC
        LIMIT :limit
    ''',
    "violations_by_zone": '''
        SELECT * FROM violations
        WHERE zone = :zone
        AND date >= :start_date
        LIMIT :limit
    ''',
}

# LLM 只選擇模板和填入參數
@tool
def query_violations(
    query_type: Literal["recent_violations", "violations_by_zone"],
    start_date: str,
    zone: Optional[str] = None,
    limit: int = 100
) -> list:
    '''根據預定義模板查詢違規記錄'''
    if limit > 100:
        limit = 100  # 強制限制

    template = QUERY_TEMPLATES[query_type]
    return db.execute(template, {
        "start_date": start_date,
        "zone": zone,
        "limit": limit
    })
```

關鍵原則：
- SQL 由開發者預先定義，不由 LLM 生成
- LLM 只能選擇模板和填入參數
- 參數有驗證和限制
"""
    print(corrections)

    # === 修正後架構圖 ===

    print("\n3. 修正後架構圖")
    print("-" * 50)

    corrected_architecture = """
修正後的系統架構：

┌────────────────────────────────────────────────────────────────┐
│                          用戶請求                               │
└────────────────────────────┬───────────────────────────────────┘
                             │
                             ▼
┌────────────────────────────────────────────────────────────────┐
│                       API Gateway                               │
│                    (驗證、限流、日誌)                            │
└────────────────────────────┬───────────────────────────────────┘
                             │
                             ▼
┌────────────────────────────────────────────────────────────────┐
│                     LLM Orchestration                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                      LangChain                            │  │
│  │                                                           │  │
│  │  ┌─────────┐    ┌─────────┐    ┌─────────┐              │  │
│  │  │  Chain  │    │  Agent  │    │  Chain  │              │  │
│  │  │ (分析)  │ →  │ (決策)  │ →  │ (生成)  │              │  │
│  │  └─────────┘    └────┬────┘    └─────────┘              │  │
│  │                      │                                    │  │
│  └──────────────────────┼────────────────────────────────────┘  │
│                         │                                       │
│         ┌───────────────┼───────────────┐                      │
│         │               │               │                      │
│         ▼               ▼               ▼                      │
│    ┌─────────┐    ┌─────────┐    ┌─────────┐                  │
│    │  Tool   │    │  Tool   │    │  Tool   │                  │
│    │ (查詢)  │    │ (通知)  │    │ (記錄)  │                  │
│    └────┬────┘    └────┬────┘    └────┬────┘                  │
│         │               │               │                      │
└─────────┼───────────────┼───────────────┼──────────────────────┘
          │               │               │
          ▼               ▼               ▼
┌────────────────┐ ┌────────────┐ ┌────────────────┐
│   資料存取層    │ │  通知服務   │ │    審計日誌    │
│  (ORM/API)     │ │  (規則化)   │ │               │
└───────┬────────┘ └─────┬──────┘ └───────┬────────┘
        │                │                 │
        ▼                ▼                 ▼
┌────────────────┐ ┌────────────┐ ┌────────────────┐
│     資料庫     │ │ Email/SMS  │ │    日誌資料庫   │
└────────────────┘ └────────────┘ └────────────────┘


LLM 責任邊界：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────────────┐
│  LLM 可以做的            │  LLM 不應該做的                   │
├─────────────────────────────────────────────────────────────┤
│  ✓ 理解用戶意圖          │  ✗ 直接執行 SQL                   │
│  ✓ 分析文字內容          │  ✗ 決定是否發送通知                │
│  ✓ 生成回應文字          │  ✗ 直接存取敏感資料                │
│  ✓ 選擇要使用的 Tool     │  ✗ 執行未經授權的操作              │
│  ✓ 填入 Tool 參數        │  ✗ 繞過權限檢查                   │
└─────────────────────────────────────────────────────────────┘
"""
    print(corrected_architecture)

    print("\n=== 完成 ===")


if __name__ == "__main__":
    main()
